# Local RAG Workbench — Python dependencies
# Install via: pip install -r requirements.txt
# Or use the automated script: bash scripts/run_backend.sh
#
# This file mirrors backend/requirements.txt.
# The backend virtual environment (.venv) is created inside backend/
# by run_backend.sh or run.sh; you do not need to install from this
# file manually unless you prefer a flat project layout.

fastapi>=0.111.0
uvicorn>=0.30.0
requests>=2.32.0
python-dotenv>=1.0.0
scikit-learn>=1.5.0
numpy>=1.26.0
